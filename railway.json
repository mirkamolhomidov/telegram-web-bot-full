{
    "build": {
        "builder": "nixpacks",
        "plan": "nodejs",
        "buildCommand": "cd server && corepack enable && npm install"
    },
    "deploy": {
        "startCommand": "cd server && node index.js"
    }
}